<div class="nav">
    <button class="toggle hamburger hamburger--arrow is-active" type="button">
      <label for="toggle">
        <span class="hamburger-box">
          <span class="hamburger-inner">  </span>
        </span>
      </label>
    </button>
    <input type="checkbox" id="toggle"/>
    <div class="menu">
      <div class="part-menu logo-sd">
         <p><%= link_to image_tag('sampledelivery2.png'), articles_path, class:"logo-sd" %></p>
      </div>

      <div class="part-menu">
        <p><%= link_to "RELEASES", realreleases_path, class:"menu-link" %></p>
      </div>
      <div class="part-menu">
        <p><%= link_to "ARTISTS", artists_path, class:"menu-link"%></p>
      </div>
      <div class="part-menu">
        <p><%= link_to "BOOKING", new_djset_path, class:"menu-link" %></p>
      </div>
      <!-- Display more link for Admin-->
      <% if user_signed_in? %>
        <% if current_user.admin? %>
      <%= link_to djsets_path do %>
      <div class="part-menu request">
        <div class="number-of-request"><p> &nbsp; &nbsp;<%= @djset_request_pending %></p></div>
        <p class="menu-link" class="z-index10">DJ SETS </p>
      </div>
      <% end %>
      <div class="part-menu">
        <p><%= link_to "CALENDAR", calendar_events_path, class:"menu-link" %></p>
      </div>
      <div class="part-menu">
        <p><%= link_to "EVENTS", events_path, class:"menu-link" %></p>
      </div>
      <%end%>
      <%end%>
      <!-- Display more link for Admin END-->

      <!-- Link for sign in sign out-->
      <% if user_signed_in? %>
        <div class="part-menu">
          <p><%= link_to "SIGN OUT", destroy_user_session_path, method: :delete %></p> </div>
        <% else %>
          <div class="part-menu">
            <p><%= link_to "SIGNIN", new_user_session_path  %></p>
          </div>
      <% end %>
      <!-- Link for sign in sign out-->

      <!-- Display logo admin if he's an admin-->
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <p style="color:red; font-size:16px;margin-top:15px;">ADMIN</p>
        <%else %>
          <p>User normal</p>
        <%end%>
        <%else%>
          <p></p>
      <%end%>
      <!-- Display logo admin if he's an admin END-->
      <div class="part-menu message">
        <p><%= link_to image_tag('message.png'), new_contact_path, class:"message-icon" %></p>
      </div>
    </div>

    <!-- Mailing success-->
    <% if flash[:success] %>
    <div id="hideMe">
      <div class= "email ">
        <div class="success">
          <p id="text" >
            <%= flash[:success] %>
          </p>
        </div>
      </div>
    </div>
    <% end %>
    <!-- Mailing success end-->
</div>
