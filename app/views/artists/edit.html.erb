<div class="container-edit">
  <div class="carreblanc">
    <div class="form">
      <%= form_for @artist do |f| %>
        <%= f.fields_for :realreleases, Realrelease.new do |realrelease_form| %>
        <div class="row-edit">
          <p>Add a release</p>
          <p>for  <%= @artist.name %></p>
          <p>
            <%= realrelease_form.text_field :name %>

          </p>
        </div>
        <% end %>

        <%= f.submit 'Add release' %>

    </div>
    <% end %>
  </div>
</div>
